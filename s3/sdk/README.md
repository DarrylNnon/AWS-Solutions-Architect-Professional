# create an sdk

# create 2 folders:
- ruby
- java

# create an s3.rb
- s3.rb

# cd into ruby
create bundle for Gemfile

```sh
bundle init
bundle install
bundle exec ruby s3.rb
BUCKET_NAME='my-cool-bucket' bundle exec s3.rb
```